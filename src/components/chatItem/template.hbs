<div class="chat-item__avatar">
  {{{ UserAvatar }}}
</div>
<div class="chat-item__content">
  <div class="chat-item__header">
    <span class="chat-item__name">{{title}}</span>
    <div class="chat-item__infoPart">
      <span class="chat-item__time">{{time}}</span>
      <button class="chat-item__deleteBtn" onclick={{onRemove}}>
        <span class="chat-item__deleteLine"></span>
        <span class="chat-item__deleteLine"></span>
      </button>
    </div>
  </div>
  <div class="chat-item__body">
    <div class="chat-item__bodyText">
      {{#if isOwnMessage }}
        <span class="chat-item__ownPrefix">Вы:</span>
      {{/if}}
      <span class="chat-item__text">{{content}}</span>
    </div>
    {{#if unread_count}}
      <div class="chat-item__unreadCount">{{unread_count}}</div>
    {{/if}}
  </div>
</div>
